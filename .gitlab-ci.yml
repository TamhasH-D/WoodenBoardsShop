# GitLab CI/CD Pipeline для Diplom Project
# Комплексная система тестирования с функциональными тестами

stages:
  - validate
  - build
  - test
  - functional-test
  - security
  - deploy

variables:
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: "/certs"
  COMPOSE_PROJECT_NAME: diplom-ci
  FUNCTIONAL_TESTS_PROJECT_NAME: diplom-functional-tests

# Базовый образ для Docker операций
.docker-base: &docker-base
  image: docker:24.0.5
  services:
    - docker:24.0.5-dind
  before_script:
    - docker info
    - docker-compose --version

# Базовый образ для Python операций
.python-base: &python-base
  image: python:3.12-slim
  before_script:
    - pip install --upgrade pip
    - apt-get update && apt-get install -y curl

# ==========================================
# VALIDATION STAGE
# ==========================================

validate:project-structure:
  stage: validate
  <<: *docker-base
  script:
    - echo "🔍 Проверка структуры проекта..."
    - |
      # Проверка основных файлов
      required_files=(
        "docker-compose.yaml"
        "Makefile"
        "README.md"
        "functional_tests/docker-compose.test.yaml"
        "functional_tests/Dockerfile"
        "functional_tests/requirements.txt"
      )
      
      for file in "${required_files[@]}"; do
        if [ ! -f "$file" ]; then
          echo "❌ Отсутствует обязательный файл: $file"
          exit 1
        fi
        echo "✅ Найден файл: $file"
      done
    
    - echo "🐳 Проверка Docker Compose конфигурации..."
    - docker-compose config --quiet
    - docker-compose -f functional_tests/docker-compose.test.yaml config --quiet
    
    - echo "📋 Проверка структуры функциональных тестов..."
    - |
      test_dirs=(
        "functional_tests/api_tests"
        "functional_tests/browser_tests"
        "functional_tests/integration_tests"
        "functional_tests/utils"
        "functional_tests/base"
      )
      
      for dir in "${test_dirs[@]}"; do
        if [ ! -d "$dir" ]; then
          echo "❌ Отсутствует директория тестов: $dir"
          exit 1
        fi
        echo "✅ Найдена директория: $dir"
      done
    
    - echo "✅ Валидация структуры проекта завершена успешно"
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "web"

# ==========================================
# BUILD STAGE
# ==========================================

build:all-services:
  stage: build
  <<: *docker-base
  script:
    - echo "🏗️ Сборка всех Docker образов..."
    - docker-compose build --parallel
    - echo "✅ Сборка основных сервисов завершена"
    
    - echo "🧪 Сборка образа функциональных тестов..."
    - cd functional_tests
    - docker build -t functional-tests:$CI_COMMIT_SHA .
    - echo "✅ Сборка тестового образа завершена"
  artifacts:
    reports:
      dotenv: build.env
    expire_in: 1 hour
  dependencies:
    - validate:project-structure
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "web"

# ==========================================
# TEST STAGE
# ==========================================

test:integration:
  stage: test
  <<: *docker-base
  script:
    - echo "🧪 Запуск интеграционных тестов..."
    - docker-compose up -d
    - echo "⏳ Ожидание готовности сервисов..."
    - sleep 30
    
    - echo "🔍 Проверка здоровья сервисов..."
    - |
      services=(
        "http://localhost:8000/api/v1/health"
        "http://localhost:8080"
        "http://localhost:8081"
        "http://localhost:8082"
      )
      
      for service in "${services[@]}"; do
        echo "Проверка: $service"
        curl -f "$service" || echo "⚠️ Сервис недоступен: $service"
      done
    
    - echo "✅ Интеграционные тесты завершены"
  after_script:
    - docker-compose logs
    - docker-compose down -v
  dependencies:
    - build:all-services
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "web"

test:backend-quality:
  stage: test
  <<: *python-base
  script:
    - echo "🔍 Проверка качества backend кода..."
    - cd backend/backend
    - pip install -r requirements.txt || pip install uv && uv sync
    - echo "🧹 Запуск линтера..."
    - ruff check . || echo "⚠️ Найдены проблемы линтинга"
    - echo "🔍 Проверка типов..."
    - mypy . || echo "⚠️ Найдены проблемы типизации"
    - echo "✅ Проверка качества backend завершена"
  dependencies:
    - build:all-services
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "web"

# ==========================================
# FUNCTIONAL TEST STAGE
# ==========================================

functional-test:api:
  stage: functional-test
  <<: *docker-base
  script:
    - echo "🔌 Запуск функциональных API тестов..."
    - cd functional_tests
    - TEST_TYPE=api docker-compose -f docker-compose.test.yaml up --build --abort-on-container-exit --exit-code-from functional-tests
    - echo "✅ API тесты завершены успешно"
  after_script:
    - cd functional_tests
    - docker-compose -f docker-compose.test.yaml logs
    - docker-compose -f docker-compose.test.yaml down -v
  artifacts:
    reports:
      junit: functional_tests/reports/api_tests_report.xml
    paths:
      - functional_tests/reports/
      - functional_tests/screenshots/
    expire_in: 1 week
    when: always
  dependencies:
    - build:all-services
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "web"

functional-test:browser:
  stage: functional-test
  <<: *docker-base
  script:
    - echo "🌐 Запуск функциональных браузерных тестов..."
    - cd functional_tests
    - TEST_TYPE=browser docker-compose -f docker-compose.test.yaml up --build --abort-on-container-exit --exit-code-from functional-tests
    - echo "✅ Браузерные тесты завершены успешно"
  after_script:
    - cd functional_tests
    - docker-compose -f docker-compose.test.yaml logs
    - docker-compose -f docker-compose.test.yaml down -v
  artifacts:
    reports:
      junit: functional_tests/reports/browser_tests_report.xml
    paths:
      - functional_tests/reports/
      - functional_tests/screenshots/
    expire_in: 1 week
    when: always
  dependencies:
    - build:all-services
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "dev"
    - if: $CI_PIPELINE_SOURCE == "web"

functional-test:integration:
  stage: functional-test
  <<: *docker-base
  script:
    - echo "🔗 Запуск end-to-end интеграционных тестов..."
    - cd functional_tests
    - TEST_TYPE=integration docker-compose -f docker-compose.test.yaml up --build --abort-on-container-exit --exit-code-from functional-tests
    - echo "✅ Интеграционные тесты завершены успешно"
  after_script:
    - cd functional_tests
    - docker-compose -f docker-compose.test.yaml logs
    - docker-compose -f docker-compose.test.yaml down -v
  artifacts:
    reports:
      junit: functional_tests/reports/integration_tests_report.xml
    paths:
      - functional_tests/reports/
      - functional_tests/screenshots/
    expire_in: 1 week
    when: always
  dependencies:
    - build:all-services
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "dev"
    - if: $CI_PIPELINE_SOURCE == "web"

functional-test:full-suite:
  stage: functional-test
  <<: *docker-base
  script:
    - echo "🚀 Запуск полного набора функциональных тестов..."
    - cd functional_tests
    - TEST_TYPE=all docker-compose -f docker-compose.test.yaml up --build --abort-on-container-exit --exit-code-from functional-tests
    - echo "✅ Полный набор тестов завершен успешно"
  after_script:
    - cd functional_tests
    - docker-compose -f docker-compose.test.yaml logs
    - docker-compose -f docker-compose.test.yaml down -v
  artifacts:
    reports:
      junit: functional_tests/reports/all_tests_report.xml
      coverage_report:
        coverage_format: cobertura
        path: functional_tests/reports/coverage.xml
    paths:
      - functional_tests/reports/
      - functional_tests/screenshots/
    expire_in: 1 week
    when: always
  dependencies:
    - build:all-services
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_PIPELINE_SOURCE == "web"
  when: manual
  allow_failure: false

# ==========================================
# SECURITY STAGE
# ==========================================

security:docker-scan:
  stage: security
  <<: *docker-base
  script:
    - echo "🔒 Сканирование Docker образов на уязвимости..."
    - |
      # Установка Trivy
      curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sh -s -- -b /usr/local/bin
      
      # Сканирование основных образов
      trivy image --exit-code 0 --severity HIGH,CRITICAL backend:latest || echo "⚠️ Найдены уязвимости в backend"
      trivy image --exit-code 0 --severity HIGH,CRITICAL functional-tests:$CI_COMMIT_SHA || echo "⚠️ Найдены уязвимости в тестах"
    - echo "✅ Сканирование безопасности завершено"
  dependencies:
    - build:all-services
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "dev"
    - if: $CI_PIPELINE_SOURCE == "web"
  allow_failure: true

# ==========================================
# DEPLOY STAGE
# ==========================================

deploy:staging:
  stage: deploy
  <<: *docker-base
  script:
    - echo "🚀 Подготовка к развертыванию в staging..."
    - echo "Тегирование образов для staging..."
    - docker tag backend:latest $CI_REGISTRY_IMAGE/backend:staging-$CI_COMMIT_SHA
    - docker tag functional-tests:$CI_COMMIT_SHA $CI_REGISTRY_IMAGE/functional-tests:staging-$CI_COMMIT_SHA
    - echo "✅ Staging развертывание подготовлено"
  environment:
    name: staging
    url: https://staging.yourdomain.com
  dependencies:
    - build:all-services
    - functional-test:api
  rules:
    - if: $CI_COMMIT_BRANCH == "dev"
  when: manual

deploy:production:
  stage: deploy
  <<: *docker-base
  script:
    - echo "🚀 Подготовка к развертыванию в production..."
    - echo "Тегирование образов для production..."
    - docker tag backend:latest $CI_REGISTRY_IMAGE/backend:latest
    - docker tag backend:latest $CI_REGISTRY_IMAGE/backend:$CI_COMMIT_SHA
    - docker tag functional-tests:$CI_COMMIT_SHA $CI_REGISTRY_IMAGE/functional-tests:latest
    - echo "✅ Production развертывание подготовлено"
  environment:
    name: production
    url: https://yourdomain.com
  dependencies:
    - build:all-services
    - functional-test:full-suite
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
  when: manual
