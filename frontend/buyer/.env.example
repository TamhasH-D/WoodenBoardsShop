# Keycloak Configuration for Buyer Frontend
# These values are used by src/keycloak.js if not overridden by a runtime config
# (e.g., public/config.js) or more specific environment settings (e.g., .env.development, .env.production).

REACT_APP_KEYCLOAK_URL=http://localhost:8080
REACT_APP_KEYCLOAK_REALM=BuyerRealm
REACT_APP_KEYCLOAK_CLIENT_ID=buyer-frontend

# Note: For actual deployments, create a .env file in the frontend/buyer directory
# (it will be gitignored by default if this project was set up with Create React App,
# ensure your project's .gitignore handles .env files appropriately)
# or set these environment variables directly in your deployment environment.
#
# DO NOT COMMIT ACTUAL SECRETS OR PRODUCTION CONFIGURATION IN THIS EXAMPLE FILE.
# This file serves as a template for developers.
#
# Example for a different environment (e.g., staging):
# REACT_APP_KEYCLOAK_URL=https://keycloak.staging.example.com
# REACT_APP_KEYCLOAK_REALM=BuyerRealmStaging
# REACT_APP_KEYCLOAK_CLIENT_ID=buyer-frontend-staging
